<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <item name="android:textSize">@dimen/default_text_size</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:windowAnimationStyle">@style/MyAnimation.Activity</item>
    </style>
    <style name="MyAnimation.Activity" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/anim_activity_translate_in</item>
        <item name="android:activityCloseExitAnimation">@anim/anim_activity_translate_out</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- ユーザー詳細のパラメータラベルテキスト -->
    <style name="UserInfoLabelText">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">0.4</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">end</item>
    </style>

    <!-- ユーザー詳細のパラメータテキスト -->
    <style name="UserInfoValueText">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">0.6</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">@dimen/detail_item_space</item>
        <item name="android:gravity">start</item>
    </style>
</resources>
